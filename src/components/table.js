import React from 'react';
import { Typography } from '@material-ui/core';

export const Table = () => {
  return (
    <div>
      <Typography variant="h4">Тут будет таблица</Typography>
    </div>
  );
};
