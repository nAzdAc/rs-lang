import React from 'react';
import { Typography } from '@material-ui/core';

export const Graph = () => {
  return (
    <div>
      <Typography variant="h4">Тут будет график</Typography>
    </div>
  );
};
